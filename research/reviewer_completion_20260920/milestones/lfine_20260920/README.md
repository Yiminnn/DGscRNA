# Lfine evaluation and native-R embedding milestone

Source archive only; the complete reviewer campaign is still running. B terminal
evaluation (408 rows) and A2 (121 samples,32 contrasts) have independent acceptance.
A1 production fitting is released; original-output validation and the separate
Lfine evaluation each have four accepted pilot units, not full-cohort acceptance.

The compatible-target-set Lfine endpoint is explicitly defined in each source
protocol. Broad marker calls are not claimed to resolve exact fine subtypes.
Every cell remains in confusion counts, and support20 limits the macro-average
class list. New K/lambda choices use training-patient Lfine scores. Original
scientific outputs and unsuccessful ICA attempts remain preserved at site paths.

These scripts retain absolute site paths and depend on external saved inputs.
Copying source and provenance does not make them a portable arbitrary-input
package. SOURCE_MANIFEST.json binds each byte to its actual execution source.
The separate research/reference_examples directory contains the verified fixed
GBM clean-environment fixture; its scope is not expanded by this archive.

Current report policy is local-only, concise Lfine results, actual all-retained-
gene main figures and later HVG comparisons. Historical policy instructions in
the older approved plan are superseded by the recorded display/delivery policy.
PTC/public computation still requires full GBM and notebook acceptance. Historical
SignacX None/0T is preserved. This archive contains no result matrices, cell-level
predictions, notebooks, environment installations, or publication credentials.
